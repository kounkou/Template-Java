import org.junit.Test;
import static org.junit.Assert.assertEquals;

/**
  This is the TemplateUnitTest class
  */
public class TemplateUnitTest {
   /**
     This is a dummy test that will instanciate an object of the Template class
     The test does nothing in particular.
     */
   @Test
   public void evaluate() {
      Template g = new Template();
      assertEquals(1, 1);
   }
}
